// rite a program which accepts a sequence of comma-separated numbers from console and generate a list and a tuple which contains every number.
// Suppose the following input is supplied to the program:

const generateArrayFromParams = (...params) => {
  return params;
}

console.log(generateArrayFromParams(1,2,3))
